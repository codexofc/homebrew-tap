# Homebrew tap for Kept

```sh
brew install codexofc/tap/kept
```

The formula installs the prebuilt `kept` binary of the latest
[release](https://github.com/codexofc/kept/releases) for macOS (Apple silicon and
Intel) and Linux (x86_64 and aarch64). Then run `kept init`.
