# Homebrew tap for Engrams

```sh
brew install codexofc/tap/engram
```

The formula installs the prebuilt `engram` binary of the latest
[release](https://github.com/codexofc/engrams/releases) for macOS (Apple silicon and
Intel) and Linux (x86_64 and aarch64). Then run `engram init`.
